# @eserstack/ajan-wasm

WebAssembly fallback for the [@eserstack/ajan](../ajan) FFI bridge, used when no
native library is available for the platform. The `.wasm` modules are build
outputs written here by `pkg/@eserstack/ajan/scripts/build.ts --wasm` and are
not committed; only this manifest is.
