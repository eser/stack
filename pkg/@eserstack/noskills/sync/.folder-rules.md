- Generated CLAUDE.md must preserve existing content outside noskills:start/end
  markers
- All command references must use the dynamic noskillsCmd prefix, never
  hardcoded
- Sync output must be idempotent — running sync twice produces identical output
- Hook scripts must be self-contained, no dependency on agent context
