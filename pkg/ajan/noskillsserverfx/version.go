package noskillsserverfx

// Version, Commit, and BuildDate are injected at build time via -X ldflags by
// GoReleaser. Defaults are used for local (non-release) builds.
var (
	Version   = "0.1.0"
	Commit    = "dev"
	BuildDate = "unknown"
)
